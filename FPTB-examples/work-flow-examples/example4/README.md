This example shows how to use fitted models (from example 4) to construct a tight binding hamiltonian

run make_hoppings.py to create a hopping.dat file which can then be used in a tight binding calculation (example1)
